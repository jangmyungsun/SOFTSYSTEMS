export default function VideoCard({video,admin=false,onToggle,onDelete}){return <article className='video-card'>{video.thumbnail_url&&<img src={video.thumbnail_url} alt=''/>}<div><p className='eyebrow'>{video.date||'Undated'} {admin?`/ ${video.is_public?'public':'private'}`:''}</p><h2>{video.title}</h2><p>{video.description}</p><p className='muted'>{(video.tags||[]).join(', ')}</p><div className='actions'><a className='button-link' href={video.youtube_url} target='_blank'>Watch</a>{admin&&<><button onClick={()=>onToggle(video)}>{video.is_public?'Make Private':'Make Public'}</button><button onClick={()=>onDelete(video)}>Delete</button></>}</div></div></article>}
