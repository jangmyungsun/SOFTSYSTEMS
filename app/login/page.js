'use client';
import {useEffect,useState} from 'react';import {supabase} from '../../lib/supabaseClient';
export default function LoginPage(){const[session,setSession]=useState(null),[email,setEmail]=useState(''),[password,setPassword]=useState('');useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));const {data:sub}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>sub.subscription.unsubscribe();},[]);const signIn=async()=>{const {error}=await supabase.auth.signInWithPassword({email,password});if(error)alert(error.message);else window.location.href='/admin';};const signOut=async()=>{await supabase.auth.signOut();setSession(null);};return <section className="panel"><h2>{session?'Account':'Login'}</h2>{session?<><p>Signed in as {session.user.email}</p><div className="actions"><button onClick={signOut}>Sign out</button></div></>:<><p className="muted">Use the owner account created in Supabase Auth.</p><div className="grid two"><label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label></div><div className="actions"><button className="primary" onClick={signIn}>Sign in</button></div></>}</section>;}
