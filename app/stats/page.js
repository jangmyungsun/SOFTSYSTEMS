'use client';
import {useEffect,useState} from 'react';import {supabase} from '../../lib/supabaseClient';import {getHomeState,parseWorkHours,getLearningHours} from '../../lib/utils';
export default function StatisticsPage(){const[logs,setLogs]=useState([]);useEffect(()=>{supabase.from('field_logs').select('*').eq('is_public',true).then(({data})=>setLogs(data||[]));},[]);const h=getHomeState(logs),making=logs.reduce((s,l)=>s+parseWorkHours(l.work?.time),0),learning=logs.reduce((s,l)=>s+getLearningHours(l),0);return <section className="grid four"><div className="panel"><p className="label">Making</p><div className="big">{making.toFixed(1)}h</div><p className="muted">total public</p></div><div className="panel"><p className="label">Learning</p><div className="big">{learning.toFixed(1)}h</div><p className="muted">total public</p></div><div className="panel"><p className="label">Body Weather</p><div className="big">{h.bodyWeather}</div></div><div className="panel"><p className="label">Energy Tone</p><div className="big">{h.energyTone}</div></div></section>;}
