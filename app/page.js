'use client';import {useEffect,useState} from 'react';import {supabase} from '../lib/supabaseClient';import {getHomeState} from '../lib/utils';import EntryCard from '../components/EntryCard';import VideoCard from '../components/VideoCard';
export default function Home(){const[logs,setLogs]=useState([]),[videos,setVideos]=useState([]);useEffect(()=>{supabase.from('field_logs').select('*').eq('is_public',true).order('date',{ascending:false}).then(({data})=>setLogs(data||[]));supabase.from('video_archive').select('*').eq('is_public',true).order('date',{ascending:false}).then(({data})=>setVideos(data||[]))},[]);const h=getHomeState(logs);return <><section className='grid four'><div className='panel'><p className='label'>Practice Rhythm</p><div className='big'>{h.making.toFixed(1)}h</div><p className='muted'>Making / day this month</p><p className='muted'>Learning {h.learning.toFixed(1)}h / day</p></div><div className='panel'><p className='label'>Body Weather</p><div className='big'>{h.bodyWeather}</div><p className='muted'>this week</p></div><div className='panel'><p className='label'>Energy Tone</p><div className='big'>{h.energyTone}</div><p className='muted'>this week</p></div><div className='panel'><p className='label'>Current Mode</p><div className='big'>{h.mode}</div></div></section><section className='panel'><h2>Latest Daily</h2>{logs.slice(0,2).map(x=><EntryCard key={x.id} log={x}/>)}</section><section className='panel'><h2>Latest Archive</h2><div className='video-grid'>{videos.slice(0,3).map(x=><VideoCard key={x.id} video={x}/>)}</div></section></>}
