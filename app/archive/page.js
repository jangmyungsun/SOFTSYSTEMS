'use client';import {useEffect,useState} from 'react';import {supabase} from '../../lib/supabaseClient';import VideoCard from '../../components/VideoCard';
export default function Archive(){const[v,setV]=useState([]);useEffect(()=>{supabase.from('video_archive').select('*').eq('is_public',true).order('date',{ascending:false}).then(({data})=>setV(data||[]))},[]);return <section className='panel'><h2>Archive</h2><p className='subtitle'>Videos made from looking, living, and thinking.</p><div className='video-grid'>{v.map(x=><VideoCard key={x.id} video={x}/>)}</div>{!v.length&&<p className='muted'>No public videos yet.</p>}</section>}
